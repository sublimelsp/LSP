import { Observable } from "@reactivex/rxjs";
import { Glob } from "glob";

// Observable.range(0, 5).forEach((l) => console.log(l.toString()));
const asdf: string = "asf";
asdf.replace("asdf", "asd");
import {harlo} from "./util";
harlo();
const glob = new Glob("asdf");
glob.on("asdf", () => { /* nop */});

// const name = process.stdin.read(2);

const obs = Observable.from([]);
obs.forEach(() => { /* nop */ });
// const str = process.stdin.read(3);

// interface ISurface {
//     corners(): number;
// }

// class Shape implements ISurface {
    // corners(): number {
    //     throw new Error("Method not implemented.");
    // }

// }
