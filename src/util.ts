/**
 * This is how the yolo works.
 * It does a lot of things, including calming the monkeys
 * @return {string} the fruit of yolo
 */
export function yolo(): string {
    return "bananas";
}

yolo();

const bi = Math.PI;
export const num: number = 55;
export const text: string = "asdf";

text.charAt(2);
